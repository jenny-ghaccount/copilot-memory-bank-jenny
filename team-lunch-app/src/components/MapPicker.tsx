'use client'\n\nimport dynamic from 'next/dynamic'\nimport { Box, Typography } from '@mui/material'\n\n// Create a dynamic import wrapper for the entire map\nconst DynamicMap = dynamic(() => import('./MapPickerInternal'), {\n  ssr: false,\n  loading: () => (\n    <Box \n      sx={{ \n        height: 400, \n        bgcolor: 'grey.100', \n        display: 'flex', \n        alignItems: 'center', \n        justifyContent: 'center',\n        borderRadius: 1,\n      }}\n    >\n      <Typography color=\"text.secondary\">Loading map...</Typography>\n    </Box>\n  ),\n})\n\ninterface Location {\n  lat: number\n  lng: number\n  address?: string\n}\n\ninterface MapPickerProps {\n  value?: Location\n  onChange: (location: Location) => void\n  height?: number\n}\n\nexport function MapPicker({ value, onChange, height = 400 }: MapPickerProps) {\n  return (\n    <Box sx={{ height, borderRadius: 1, overflow: 'hidden' }}>\n      <DynamicMap value={value} onChange={onChange} height={height} />\n    </Box>\n  )\n}"